# Components

This directory contains all React components for the Zerothlayer application.

## Structure (to be created)

- **Canvas** - Core canvas rendering and interaction
- **Toolbar** - Top toolbar with actions
- **LayerPanel** - Layer management UI (future)
- **SelectionTools** - Rectangle, lasso, magic select tools (future)

Keep components focused, reusable, and well-typed.
